<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leaderboard - FUNLEARN</title>
    <link rel="stylesheet" href="/tailwind.css">
</head>
<body class="bg-gray-50">
    {{> header}}

    <div class="container mx-auto px-6 py-8">
        <div class="max-w-4xl mx-auto">
            <h1 class="text-4xl font-bold mb-8 text-center">🏆 Leaderboard</h1>

            <!-- Top 3 Podium -->
            {{#if students.length}}
            <div class="text-center mb-8">
                <div class="inline-flex items-end space-x-4">
                    <!-- Second Place -->
                    {{#if students.[1]}}
                    <div class="bg-gradient-to-br from-gray-300 to-gray-400 text-white rounded-lg p-4 text-center">
                        <div class="text-4xl mb-2">🥈</div>
                        <div class="text-2xl font-bold mb-2">#2</div>
                        <h3 class="text-lg font-bold">{{students.[1].first_name}}</h3>
                        <p class="text-gray-100">{{students.[1].xp}} XP</p>
                    </div>
                    {{/if}}
                    
                    <!-- First Place -->
                    {{#if students.[0]}}
                    <div class="bg-gradient-to-br from-yellow-400 to-yellow-600 text-white rounded-lg p-6 text-center transform scale-110">
                        <div class="text-6xl mb-2">👑</div>
                        <div class="text-4xl font-bold mb-2">#1</div>
                        <h3 class="text-xl font-bold">{{students.[0].first_name}} {{students.[0].last_name}}</h3>
                        <p class="text-yellow-100">Level {{students.[0].level}}</p>
                        <p class="text-3xl font-bold mt-4">{{students.[0].xp}} XP</p>
                    </div>
                    {{/if}}
                    
                    <!-- Third Place -->
                    {{#if students.[2]}}
                    <div class="bg-gradient-to-br from-orange-400 to-orange-600 text-white rounded-lg p-4 text-center">
                        <div class="text-4xl mb-2">🥉</div>
                        <div class="text-2xl font-bold mb-2">#3</div>
                        <h3 class="text-lg font-bold">{{students.[2].first_name}}</h3>
                        <p class="text-orange-100">{{students.[2].xp}} XP</p>
                    </div>
                    {{/if}}
                </div>
            </div>
            {{/if}}

            <!-- Complete Leaderboard -->
            <div class="bg-white rounded-lg shadow">
                <div class="p-6">
                    <h2 class="text-2xl font-bold mb-4">🏆 Complete Rankings</h2>
                    {{#if students.length}}
                    <div class="space-y-2">
                        {{#each students}}
                        <div class="flex items-center justify-between p-4 border rounded hover:bg-gray-50">
                            <div class="flex items-center space-x-4">
                                <div class="text-2xl font-bold w-12 text-gray-600">
                                    #{{add @index 1}}
                                </div>
                                <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full flex items-center justify-center text-white font-bold">
                                    {{substring this.first_name 0 1}}{{substring this.last_name 0 1}}
                                </div>
                                <div>
                                    <p class="font-semibold text-gray-800">{{this.first_name}} {{this.last_name}}</p>
                                    <p class="text-sm text-gray-500">Level {{this.level}} • {{this.streak_days}} day streak</p>
                                </div>
                            </div>
                            <div class="text-right">
                                <p class="text-xl font-bold text-blue-600">{{this.xp}} XP</p>
                                <div class="flex items-center justify-end mt-1">
                                    <div class="w-20 bg-gray-200 rounded-full h-2 mr-2">
                                        <div class="bg-blue-600 h-2 rounded-full" style="width: 75%"></div>
                                    </div>
                                    <span class="text-xs text-gray-500">75%</span>
                                </div>
                            </div>
                        </div>
                        {{/each}}
                    </div>
                    {{else}}
                    <div class="text-center py-12">
                        <div class="text-6xl mb-4 opacity-50">🏆</div>
                        <p class="text-gray-500 mb-4">No leaderboard data available</p>
                        <p class="text-gray-400 text-sm">Complete lessons and earn XP to appear on the leaderboard!</p>
                    </div>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</body>
</html>
