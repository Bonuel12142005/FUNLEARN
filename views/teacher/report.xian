<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Class Report - {{classData.class_name}} - FUNLEARN</title>
    <link rel="stylesheet" href="/tailwind.css">
    <link rel="stylesheet" href="/funlearn-styles.css">
</head>
<body class="bg-gray-50">
    {{> header}}

    <div class="container mx-auto px-6 py-8">
        <!-- Header -->
        <div class="flex items-center justify-between mb-8">
            <div>
                <h1 class="text-4xl font-bold text-gray-800">Class Report</h1>
                <p class="text-gray-600 mt-2">{{classData.class_name}} - Comprehensive performance report</p>
            </div>
            <div class="flex items-center space-x-3">
                <button class="btn btn-secondary">
                    <i class="fas fa-download mr-2"></i>Export PDF
                </button>
                <a href="/teacher/classes/{{classData.id}}" class="btn btn-primary">
                    <i class="fas fa-arrow-left mr-2"></i>Back to Class
                </a>
            </div>
        </div>

        <!-- Report Summary -->
        <div class="card p-6 mb-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-6">📊 Report Summary</h2>
            <div class="grid md:grid-cols-3 gap-6">
                <div class="text-center">
                    <div class="text-3xl font-bold text-blue-600 mb-2">{{classData.students.length}}</div>
                    <p class="text-gray-600">Total Students</p>
                </div>
                <div class="text-center">
                    <div class="text-3xl font-bold text-green-600 mb-2">85%</div>
                    <p class="text-gray-600">Average Completion</p>
                </div>
                <div class="text-center">
                    <div class="text-3xl font-bold text-purple-600 mb-2">4.2</div>
                    <p class="text-gray-600">Average Rating</p>
                </div>
            </div>
        </div>

        <!-- Detailed Report -->
        <div class="card p-6">
            <h2 class="text-2xl font-bold text-gray-800 mb-6">📈 Detailed Performance</h2>
            
            {{#if reportData}}
                <div class="space-y-6">
                    {{#each reportData}}
                    <div class="border-l-4 border-blue-500 pl-4">
                        <h3 class="font-semibold text-gray-800">{{this.title}}</h3>
                        <p class="text-gray-600 mt-1">{{this.description}}</p>
                    </div>
                    {{/each}}
                </div>
            {{else}}
                <div class="text-center py-12">
                    <div class="text-6xl mb-4 opacity-50">📊</div>
                    <p class="text-gray-500">Report data is being generated...</p>
                    <p class="text-gray-400 text-sm mt-2">Please check back later for detailed analytics</p>
                </div>
            {{/if}}
        </div>
    </div>
</body>
</html>